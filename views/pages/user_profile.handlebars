<!DOCTYPE html>
<html>

<head>
  <title>
    My Project | Profile
  </title>
  {{> externals }}
</head>

<body>
  {{> header }}

  <div class="container">
    <h1 class="text-center m-3">
      {{title}}
    </h1>
    <div class="col-md-12">
      <div class="card col-md-6 offset-3">
        <br>
        <img class="card-img-top w-50 mx-auto border rounded-circle" src="{{image}}">
        <div class="card-block">
          <h4 class="card-title text-center mt-2">Your profile</h4>

          <ul class="list-group text-center">
            <li class="list-group-item">Name: {{user.local.firstName}}</li>
            <li class="list-group-item">Email: {{user.local.lastName}}</li>
            {{#unless isEmployer}}
            <li class="list-group-item">Skills: {{user.skills}}</li>
            {{/unless}}
          </ul>
        </div>
      </div>
    </div>
  </div>

  {{> footer }}
</body>